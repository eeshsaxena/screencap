functions-framework==3.*
# firebase-admin 7.4.0 requires google-cloud-storage>=3.1.1 — this is a forced
# major bump from the previous >=2.14.0 pin. The 3.x line is API-compatible for
# generate_signed_url/list_blobs but changes checksum defaults (crc32c), enables
# retries by default, and relocates resumable-media exceptions; the upload-
# checksum path is re-tested against the real 3.x library in test_signing_contract.py
# (SCR-140: signing stays checksum-free — crc32c="auto" only affects upload_from_*/
# download_to_* transfer methods, which this signing-only function never calls) and the
# migration's per-object crc32c verify (U8) runs on this library.
google-cloud-storage>=3.1.1
# Offline Firebase ID-token verification (bearer -> uid). Uses ADC on Cloud Run;
# check_revoked=False keeps verification on the hot path (no Admin-API call).
firebase-admin>=7.4.0
# Billing: hosted Checkout session creation + webhook signature verification for
# the $5/mo Personal-cloud paywall (billing.py). Only the billing entry points
# import it; the signing function does not.
stripe>=9,<12
